<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>AMI | EC2</title>
    <link rel="stylesheet" href="/css/images.css" />
  </head>
  <body>
    <%- include('../base.ejs') %>

    <h2>AMI</h2>

    <div class="card-container">
      <% images.forEach(image => { %>
      <div class="card">
        <div class="card-header">
          <h3 class="card-title"><%= image.Name %></h3>
          <span
            class="status-badge <%= image.State === 'available' ? 'status-available' : 'status-unavailable' %>"
          >
            <%= image.State %>
          </span>
        </div>
        <div class="card-body">
          <p><strong>AMI ID:</strong> <%= image.ImageId %></p>
          <p><strong>아키텍쳐:</strong> <%= image.Architecture %></p>
          <p>
            <strong>생성일자:</strong> <%= new
            Date(image.CreationDate).toLocaleString() %>
          </p>
          <p><strong>설명:</strong> <%= image.Description %></p>
          <p>
            <strong>루트 디바이스 유형:</strong> <%= image.RootDeviceType %>
          </p>
          <p><strong>가상화 유형:</strong> <%= image.VirtualizationType %></p>
          <p><strong>소유자 ID:</strong> <%= image.OwnerId %></p>
          <p>
            <strong>소스 인스턴스 ID:</strong> <%= image.SourceInstanceId %>
          </p>
        </div>
      </div>
      <% }); %>
    </div>
  </body>
</html>
